@inherits MainDataListItemComponentBase<RoleViewModel>

<MainDataListItemTemplate TViewModel="@RoleViewModel"
                          Item="@Item"
                          Context="@Context"
                          ItemUri="@(r => $"main-data/roles/{r.Id}")"
                          ItemTitle="@(r => r.Name)"
                          TopDetails="@GetBottomDetails()"
                          OnDelete="@OnDeleteAsync" />
